# Frontend (Vite). Opcional.
# En desarrollo NO hace falta: Vite proxea "/api" al backend (ver vite.config.ts).
# Úsala solo si sirves el build de producción y el backend está en otra URL.
# VITE_API_URL=http://localhost:8787
